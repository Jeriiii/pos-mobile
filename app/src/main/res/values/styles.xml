<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="POStheme"/>

    <style name="AppTheme" parent="AppBaseTheme">
        <!-- Customize your theme here. -->
        <item name="android:logo">@android:color/transparent</item>
    </style>

</resources>
